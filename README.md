# activerecord_callback_lens

**X-ray your ActiveRecord callbacks.**

Callbacks are easy to add and hard to reason about.


`activerecord_callback_lens` provides deep static analysis of ActiveRecord callback chains, expanding callback conditions into logical trees and visual dependency graphs so you can inspect the hidden behavior of your models.
